package com.surelution.wxmp.jx

/**
 * 好吧，这个domain的存在，仅仅是因为我在代码中拉了大便，
 * 由于使用了id作为抽奖的sn，但是，在一个同表的domain继承树中，
 * 无法实现id单独记录，只好弄出这么个表，生成编号
 * 
 * @author <a href="mailto:guangzong.syu@gmail.com">guangzong</a>
 *
 */
class OpenDay2015TicketSn {

    static constraints = {
    }
}
